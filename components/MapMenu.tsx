import { View } from "react-native"
import { COLORS } from "../consts"

const MapMenu = () =>{
    return (
        <View style={{ position: 'absolute', bottom: 0, width: '100%', height: '10%', backgroundColor: COLORS.gray }}>

        </View>
    )
}

export default MapMenu